/** @format */

import CalcForm from "../../components/calculator";
import { Layout } from '../../components/Layout';

const Calc = () => {
  return (
    <Layout>
      <h1>Calculator</h1>
      <CalcForm />
    </Layout>
  );
};

export default Calc;
